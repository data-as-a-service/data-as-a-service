# Copilot Instructions

## Project Guidelines
- User prefers git operations to target the 'org' remote as primary and wants the local workspace 'main' branch to match 'org/main'. User is fine losing local commits.
- User wants the 'personal' remote kept but updated to reflect 'org/main'; 'personal' should be synced with 'org/main'.